import React from "react";
import { Typography } from "@mui/material";

interface Props {
  content: string;
}

const TITLE = "{{pascalCase name}}";

export const {{pascalCase name}}: React.FC<Props> = ({ content }) => {
  return (
    <>
        <Typography variant="h5" sx={ { flexGrow: 1 } }>{ content }</Typography>
    </>
  );
};
